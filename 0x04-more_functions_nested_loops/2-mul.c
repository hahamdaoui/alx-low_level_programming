#include "main.h"

/**
 * mul - multiplies two number integers
 * @a: variable the type integer
 * @b: variable the type integer
 * Return: always 0
 */

int mul(int a, int b)
{
	return (a * b);
	_putchar('\n');
}
